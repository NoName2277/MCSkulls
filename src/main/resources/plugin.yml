name: MCSkulls
version: '${project.version}'
main: me.noname.mcskulls.Main
api-version: '1.15'
depend:
  - Vault
  - PlaceholderAPI
commands:
  czaszki:
permissions:
  skuls.admin:
    default: op
    description: admin command

